#include "CoreSystems/DebugGuiSystem.h"

void DebugGuiSystem::initialize(entt::registry& registry) { System::initialize(registry); }

void DebugGuiSystem::update(entt::registry& registry, const float deltaTime)
{
}
